import React from "react";

interface MatchingDetailPageLayoutProps {
  children: React.ReactNode;
}

const MatchingDetailPageLayout = ({
  children,
}: MatchingDetailPageLayoutProps) => {
  return <>{children}</>;
};

export default MatchingDetailPageLayout;
